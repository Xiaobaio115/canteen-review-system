# 校园餐厅智能点评系统 — 扩展功能计划书

**文档版本：** V1.0
**生成日期：** 2026年6月8日
**适用范围：** 菜品管理、收藏、点赞、标签系统

---

## 文档说明

本文档面向开发团队，详细描述四项扩展功能的操作逻辑、API接口设计、数据库设计及Sequelize模型。所有扩展功能以**平行模块**方式新增，**不修改**现有数据库表（user、restaurant、review）和已有API接口。

### 现有模块保留声明

| 类别 | 内容 | 变更方式 |
| --- | --- | --- |
| 数据库表 | user、restaurant、review | 保持不变 |
| API接口前缀 | /api/v1 认证、餐厅、点评、推荐、商家、管理员、上传 | 保持不变，不修改路由注册 |
| 后端模型 | User.js、Restaurant.js、Review.js | 保持不变 |
| 后端控制器 | auth、restaurant、review、recommendation、merchant、admin、upload | 保持不变 |
| 后端路由 | auth.routes、restaurant.routes、review.routes、recommendation.routes、merchant.routes、admin.routes、upload.routes | 保持不变 |
| 前端API模块 | auth.api、restaurant.api、review.api、recommendation.api、merchant.api、admin.api、upload.api | 保持不变 |

### 扩展功能总览

| 功能 | 新增表 | 新增API数量 | 主要角色 |
| --- | --- | --- | --- |
| 菜品管理 | dish | 7 | 商家、学生 |
| 收藏功能 | favorite | 4 | 学生 |
| 点赞功能 | likes | 4 | 学生 |
| 标签系统 | tag、restaurant_tag | 10 | 管理员、商家、学生 |

---

## 一、菜品管理

### 1.1 操作逻辑

#### 商家操作
1. 商家登录后进入餐厅管理页，点击「菜品管理」。
2. **新增菜品**：填写菜品名称、价格、可选描述和图片，提交后菜品状态默认为上架（status=1）。
3. **编辑菜品**：修改菜品名称、价格、描述、图片等信息，保存后立即生效。
4. **删除菜品**：软删除，将菜品状态置为下架（status=0），前端不再显示。
5. **查看菜品列表**：分页查看自己餐厅的全部菜品，支持按状态筛选。

#### 学生操作
1. 学生进入餐厅详情页，在「菜品列表」区域浏览当前餐厅的上架菜品。
2. 按名称模糊搜索菜品。
3. 查看菜品名称、价格、描述和图片。

### 1.2 API接口

#### POST /api/v1/merchant/dishes — 新增菜品
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| restaurant_id | bigint | 是 | 菜品所属餐厅ID |
| name | string | 是 | 菜品名称（1-100字） |
| price | decimal(10,2) | 是 | 菜品价格 |
| description | string | 否 | 菜品描述 |
| image | string | 否 | 菜品图片URL |
```
请求：{"restaurantId":1,"name":"红烧肉","price":15.00,"description":"招牌菜，肥而不腻"}
返回：{"code":200,"message":"菜品创建成功","data":{"dishId":1,"name":"红烧肉","price":15.00}}
```
**权限：** 商家（仅限自己的餐厅）

#### PUT /api/v1/merchant/dishes/{dishId} — 编辑菜品
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| name | string | 否 | 菜品名称 |
| price | decimal(10,2) | 否 | 菜品价格 |
| description | string | 否 | 菜品描述 |
| image | string | 否 | 菜品图片URL |
| status | int | 否 | 状态：1上架，0下架 |
```
请求：{"price":18.00,"description":"招牌红烧肉，每日限量"}
返回：{"code":200,"message":"菜品更新成功","data":{"dishId":1,"name":"红烧肉","price":18.00}}
```
**权限：** 商家（仅限自己的餐厅）

#### DELETE /api/v1/merchant/dishes/{dishId} — 删除菜品
```
返回：{"code":200,"message":"菜品已删除","data":null}
```
**权限：** 商家（仅限自己的餐厅）

#### GET /api/v1/merchant/dishes?restaurantId={restaurantId} — 商家查看菜品列表
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| restaurantId | number | 是 | 餐厅ID |
| status | number | 否 | 筛选状态（1上架，0下架） |
| page | number | 否 | 页码，默认1 |
| size | number | 否 | 每页数量，默认10 |
```
返回：{"code":200,"message":"success","data":{"total":5,"page":1,"size":10,"list":[{"dishId":1,"name":"红烧肉","price":15.00,"status":1}]}}
```
**权限：** 商家（仅限自己的餐厅）

#### GET /api/v1/restaurants/{restaurantId}/dishes — 学生浏览菜品列表
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| keyword | string | 否 | 菜品名称关键词 |
| page | number | 否 | 页码，默认1 |
| size | number | 否 | 每页数量，默认10 |
```
返回：{"code":200,"message":"success","data":{"total":5,"page":1,"size":10,"list":[{"dishId":1,"name":"红烧肉","price":15.00,"description":"招牌菜"}]}}
```
**权限：** 公开/登录用户

#### GET /api/v1/dishes/{dishId} — 获取菜品详情
```
返回：{"code":200,"message":"success","data":{"dishId":1,"restaurantId":1,"name":"红烧肉","price":15.00,"description":"招牌菜，肥而不腻","image":"http://...","status":1}}
```
**权限：** 公开/登录用户

#### POST /api/v1/admin/dishes — 管理员新增菜品（跨餐厅）
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| restaurantId | bigint | 是 | 餐厅ID |
| name | string | 是 | 菜品名称 |
| price | decimal(10,2) | 是 | 菜品价格 |
| description | string | 否 | 菜品描述 |
| image | string | 否 | 菜品图片URL |
```
请求：{"restaurantId":1,"name":"宫保鸡丁","price":14.00}
返回：{"code":200,"message":"菜品创建成功","data":{"dishId":2,"name":"宫保鸡丁","price":14.00}}
```
**权限：** 管理员

### 1.3 数据库设计

#### dish表

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | 主键，自增 | 菜品ID |
| restaurant_id | bigint | 外键，非空 | 所属餐厅ID |
| name | varchar(100) | 非空 | 菜品名称 |
| price | decimal(10,2) | 非空，默认0 | 菜品价格 |
| description | text | 可空 | 菜品描述 |
| image | varchar(500) | 可空 | 菜品图片URL |
| status | int | 默认1 | 状态：1上架，0下架 |
| create_time | datetime | 非空 | 创建时间 |
| update_time | datetime | 非空 | 更新时间 |

```
CREATE TABLE dish (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  restaurant_id BIGINT NOT NULL,
  name VARCHAR(100) NOT NULL,
  price DECIMAL(10,2) NOT NULL DEFAULT 0,
  description TEXT,
  image VARCHAR(500),
  status INT NOT NULL DEFAULT 1,
  create_time DATETIME NOT NULL,
  update_time DATETIME NOT NULL,
  CONSTRAINT fk_dish_restaurant FOREIGN KEY (restaurant_id) REFERENCES restaurant(id)
);
```

### 1.4 Sequelize模型

```javascript
// server/models/Dish.js
const { DataTypes } = require('sequelize');
const { sequelize } = require('../config/database');

const Dish = sequelize.define('Dish', {
  id: {
    type: DataTypes.BIGINT,
    primaryKey: true,
    autoIncrement: true
  },
  restaurant_id: {
    type: DataTypes.BIGINT,
    allowNull: false
  },
  name: {
    type: DataTypes.STRING(100),
    allowNull: false
  },
  price: {
    type: DataTypes.DECIMAL(10, 2),
    allowNull: false,
    defaultValue: 0
  },
  description: {
    type: DataTypes.TEXT,
    allowNull: true
  },
  image: {
    type: DataTypes.STRING(500),
    allowNull: true
  },
  status: {
    type: DataTypes.INTEGER,
    defaultValue: 1
  },
  create_time: {
    type: DataTypes.DATE,
    allowNull: false,
    defaultValue: DataTypes.NOW
  },
  update_time: {
    type: DataTypes.DATE,
    allowNull: false,
    defaultValue: DataTypes.NOW
  }
}, {
  tableName: 'dish',
  timestamps: false
});

module.exports = Dish;
```

### 1.5 前端API请求示例

```javascript
// client/src/api/dish.api.js
import request from './request'

export const getDishes = (restaurantId, params) => {
  return request.get(`/restaurants/${restaurantId}/dishes`, { params })
}

export const getDishDetail = (id) => {
  return request.get(`/dishes/${id}`)
}

export const getMerchantDishes = (params) => {
  return request.get('/merchant/dishes', { params })
}

export const createDish = (data) => {
  return request.post('/merchant/dishes', data)
}

export const updateDish = (id, data) => {
  return request.put(`/merchant/dishes/${id}`, data)
}

export const deleteDish = (id) => {
  return request.delete(`/merchant/dishes/${id}`)
}
```

---

## 二、收藏功能

### 2.1 操作逻辑

1. 学生登录后进入餐厅详情页，点击「收藏」按钮收藏当前餐厅。
2. 再次点击「取消收藏」取消收藏。
3. 学生可以在个人中心「我的收藏」页面查看已收藏的餐厅列表。
4. 收藏按钮显示当前收藏状态（已收藏/未收藏）。
5. 同一学生不能重复收藏同一餐厅（唯一约束）。

### 2.2 API接口

#### POST /api/v1/favorites — 添加收藏
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| restaurantId | bigint | 是 | 餐厅ID |
```
请求：{"restaurantId":1}
返回：{"code":200,"message":"收藏成功","data":{"favoriteId":1,"restaurantId":1}}
```
**权限：** 学生

#### DELETE /api/v1/favorites/{favoriteId} — 取消收藏
```
返回：{"code":200,"message":"已取消收藏","data":null}
```
**权限：** 学生（仅自己的收藏）

#### GET /api/v1/favorites — 查看收藏列表
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| page | number | 否 | 页码，默认1 |
| size | number | 否 | 每页数量，默认10 |
```
返回：{"code":200,"message":"success","data":{"total":3,"page":1,"size":10,"list":[{"favoriteId":1,"restaurantId":1,"restaurantName":"第一食堂","avgScore":4.5,"createTime":"2026-06-01T10:00:00"}]}}
```
**权限：** 学生

#### GET /api/v1/favorites/check?restaurantId={restaurantId} — 检查收藏状态
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| restaurantId | bigint | 是 | 餐厅ID |
```
返回：{"code":200,"message":"success","data":{"favorited":true,"favoriteId":1}}
```
**权限：** 学生

### 2.3 数据库设计

#### favorite表

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | 主键，自增 | 收藏ID |
| user_id | bigint | 外键，非空 | 学生用户ID |
| restaurant_id | bigint | 外键，非空 | 餐厅ID |
| create_time | datetime | 非空 | 收藏时间 |

```sql
CREATE TABLE favorite (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  restaurant_id BIGINT NOT NULL,
  create_time DATETIME NOT NULL,
  CONSTRAINT fk_favorite_user FOREIGN KEY (user_id) REFERENCES user(id),
  CONSTRAINT fk_favorite_restaurant FOREIGN KEY (restaurant_id) REFERENCES restaurant(id),
  UNIQUE KEY uk_user_restaurant (user_id, restaurant_id)
);
```

### 2.4 Sequelize模型

```javascript
// server/models/Favorite.js
const { DataTypes } = require('sequelize');
const { sequelize } = require('../config/database');

const Favorite = sequelize.define('Favorite', {
  id: {
    type: DataTypes.BIGINT,
    primaryKey: true,
    autoIncrement: true
  },
  user_id: {
    type: DataTypes.BIGINT,
    allowNull: false
  },
  restaurant_id: {
    type: DataTypes.BIGINT,
    allowNull: false
  },
  create_time: {
    type: DataTypes.DATE,
    allowNull: false,
    defaultValue: DataTypes.NOW
  }
}, {
  tableName: 'favorite',
  timestamps: false,
  indexes: [
    {
      unique: true,
      fields: ['user_id', 'restaurant_id']
    }
  ]
});

module.exports = Favorite;
```

### 2.5 前端API请求示例

```javascript
// client/src/api/favorite.api.js
import request from './request'

export const addFavorite = (restaurantId) => {
  return request.post('/favorites', { restaurantId })
}

export const removeFavorite = (id) => {
  return request.delete(`/favorites/${id}`)
}

export const getFavorites = (params) => {
  return request.get('/favorites', { params })
}

export const checkFavorite = (restaurantId) => {
  return request.get('/favorites/check', { params: { restaurantId } })
}
```

---

## 三、点赞功能

### 3.1 操作逻辑

1. 学生浏览点评时，点击点评旁的「赞」按钮为该点评点赞。
2. 再次点击取消点赞。
3. 同一学生对同一条点评只能点赞一次（唯一约束）。
4. 点评列表和详情页显示点赞数量。
5. 学生不能给自己的点评点赞。

### 3.2 API接口

#### POST /api/v1/likes — 点赞
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| reviewId | bigint | 是 | 点评ID |
```
请求：{"reviewId":10}
返回：{"code":200,"message":"点赞成功","data":{"likeId":1,"reviewId":10}}
```
**权限：** 学生

#### DELETE /api/v1/likes/{likeId} — 取消点赞
```
返回：{"code":200,"message":"已取消点赞","data":null}
```
**权限：** 学生（仅自己的点赞）

#### GET /api/v1/reviews/{reviewId}/likes/count — 获取点评点赞数
```
返回：{"code":200,"message":"success","data":{"reviewId":10,"likeCount":23}}
```
**权限：** 公开/登录用户

#### GET /api/v1/likes/check?reviewId={reviewId} — 检查点赞状态
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| reviewId | bigint | 是 | 点评ID |
```
返回：{"code":200,"message":"success","data":{"liked":true,"likeId":1}}
```
**权限：** 学生

### 3.3 数据库设计

#### likes表

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | 主键，自增 | 点赞ID |
| user_id | bigint | 外键，非空 | 点赞学生ID |
| review_id | bigint | 外键，非空 | 被点赞点评ID |
| create_time | datetime | 非空 | 点赞时间 |

```sql
CREATE TABLE likes (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  review_id BIGINT NOT NULL,
  create_time DATETIME NOT NULL,
  CONSTRAINT fk_likes_user FOREIGN KEY (user_id) REFERENCES user(id),
  CONSTRAINT fk_likes_review FOREIGN KEY (review_id) REFERENCES review(id),
  UNIQUE KEY uk_user_review (user_id, review_id)
);
```

### 3.4 Sequelize模型

```javascript
// server/models/Like.js
const { DataTypes } = require('sequelize');
const { sequelize } = require('../config/database');

const Like = sequelize.define('Like', {
  id: {
    type: DataTypes.BIGINT,
    primaryKey: true,
    autoIncrement: true
  },
  user_id: {
    type: DataTypes.BIGINT,
    allowNull: false
  },
  review_id: {
    type: DataTypes.BIGINT,
    allowNull: false
  },
  create_time: {
    type: DataTypes.DATE,
    allowNull: false,
    defaultValue: DataTypes.NOW
  }
}, {
  tableName: 'likes',
  timestamps: false,
  indexes: [
    {
      unique: true,
      fields: ['user_id', 'review_id']
    }
  ]
});

module.exports = Like;
```

### 3.5 前端API请求示例

```javascript
// client/src/api/like.api.js
import request from './request'

export const addLike = (reviewId) => {
  return request.post('/likes', { reviewId })
}

export const removeLike = (id) => {
  return request.delete(`/likes/${id}`)
}

export const getLikeCount = (reviewId) => {
  return request.get(`/reviews/${reviewId}/likes/count`)
}

export const checkLike = (reviewId) => {
  return request.get('/likes/check', { params: { reviewId } })
}
```

---

## 四、标签系统

### 4.1 操作逻辑

#### 管理员操作
1. 管理员进入标签管理页面，查看全部标签列表。
2. **新增标签**：创建标签名称和分类（如口味、环境、服务、性价比）。
3. **编辑标签**：修改标签名称或分类。
4. **删除标签**：删除标签（级联删除餐厅-标签关联）。
5. **查看标签列表**：分页查看全部标签。

#### 商家操作
1. 商家进入餐厅管理页，点击「标签管理」。
2. **为餐厅打标**：从可选标签列表中选择标签添加到自己的餐厅。
3. **移除标签**：从餐厅移除已添加的标签。
4. 查看当前餐厅已有的标签。

#### 学生操作
1. 学生搜索餐厅时，选择一个或多个标签进行筛选。
2. 餐厅列表和详情页展示餐厅的标签。

### 4.2 API接口

#### 标签管理（管理员）

##### POST /api/v1/admin/tags — 新增标签
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| name | string | 是 | 标签名称（1-20字） |
| category | string | 否 | 标签分类，如口味、环境、服务、性价比 |
```
请求：{"name":"便宜","category":"性价比"}
返回：{"code":200,"message":"标签创建成功","data":{"tagId":1,"name":"便宜","category":"性价比"}}
```
**权限：** 管理员

##### PUT /api/v1/admin/tags/{tagId} — 编辑标签
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| name | string | 否 | 标签名称 |
| category | string | 否 | 标签分类 |
```
请求：{"name":"价格实惠"}
返回：{"code":200,"message":"标签更新成功","data":{"tagId":1,"name":"价格实惠","category":"性价比"}}
```
**权限：** 管理员

##### DELETE /api/v1/admin/tags/{tagId} — 删除标签
```
返回：{"code":200,"message":"标签已删除","data":null}
```
**权限：** 管理员

##### GET /api/v1/admin/tags — 管理员查看标签列表
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| category | string | 否 | 按分类筛选 |
| page | number | 否 | 页码，默认1 |
| size | number | 否 | 每页数量，默认20 |
```
返回：{"code":200,"message":"success","data":{"total":10,"page":1,"size":20,"list":[{"tagId":1,"name":"便宜","category":"性价比"},{"tagId":2,"name":"量大","category":"性价比"}]}}
```
**权限：** 管理员

#### 餐厅标签（商家）

##### POST /api/v1/merchant/restaurants/{restaurantId}/tags — 为餐厅添加标签
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| tagId | bigint | 是 | 标签ID |
```
请求：{"tagId":1}
返回：{"code":200,"message":"标签添加成功","data":null}
```
**权限：** 商家（仅限自己的餐厅）

##### DELETE /api/v1/merchant/restaurants/{restaurantId}/tags/{tagId} — 移除餐厅标签
```
返回：{"code":200,"message":"标签已移除","data":null}
```
**权限：** 商家（仅限自己的餐厅）

##### GET /api/v1/merchant/restaurants/{restaurantId}/tags — 商家查看餐厅标签
```
返回：{"code":200,"message":"success","data":[{"tagId":1,"name":"便宜","category":"性价比"}]
```
**权限：** 商家（仅限自己的餐厅）

#### 标签浏览（公开）

##### GET /api/v1/tags — 获取全部标签（用于筛选）
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| category | string | 否 | 按分类筛选 |
```
返回：{"code":200,"message":"success","data":{"口味":["辣","清淡","甜"],"性价比":["便宜","量大"]}}
```
**权限：** 公开/登录用户

##### GET /api/v1/restaurants?tagIds={tagIds} — 按标签筛选餐厅（扩展现有接口）
| 参数 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| tagIds | string | 否 | 逗号分隔的标签ID列表 |
```
说明：在现有餐厅列表接口增加 tagIds 查询参数。
返回：同现有餐厅列表格式，仅返回包含指定标签的餐厅。
```
**权限：** 公开/登录用户

##### GET /api/v1/restaurants/{restaurantId}/tags — 查看餐厅的标签
```
返回：{"code":200,"message":"success","data":[{"tagId":1,"name":"便宜","category":"性价比"}]
```
**权限：** 公开/登录用户

### 4.3 数据库设计

#### tag表

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | 主键，自增 | 标签ID |
| name | varchar(50) | 非空，唯一 | 标签名称 |
| category | varchar(50) | 可空 | 标签分类 |
| create_time | datetime | 非空 | 创建时间 |

#### restaurant_tag表

| 字段名 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | bigint | 主键，自增 | 关联ID |
| restaurant_id | bigint | 外键，非空 | 餐厅ID |
| tag_id | bigint | 外键，非空 | 标签ID |
| create_time | datetime | 非空 | 关联创建时间 |

```sql
CREATE TABLE tag (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(50) NOT NULL UNIQUE,
  category VARCHAR(50),
  create_time DATETIME NOT NULL
);

CREATE TABLE restaurant_tag (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  restaurant_id BIGINT NOT NULL,
  tag_id BIGINT NOT NULL,
  create_time DATETIME NOT NULL,
  CONSTRAINT fk_rt_restaurant FOREIGN KEY (restaurant_id) REFERENCES restaurant(id),
  CONSTRAINT fk_rt_tag FOREIGN KEY (tag_id) REFERENCES tag(id),
  UNIQUE KEY uk_restaurant_tag (restaurant_id, tag_id)
);
```

### 4.4 Sequelize模型

```javascript
// server/models/Tag.js
const { DataTypes } = require('sequelize');
const { sequelize } = require('../config/database');

const Tag = sequelize.define('Tag', {
  id: {
    type: DataTypes.BIGINT,
    primaryKey: true,
    autoIncrement: true
  },
  name: {
    type: DataTypes.STRING(50),
    allowNull: false,
    unique: true
  },
  category: {
    type: DataTypes.STRING(50),
    allowNull: true
  },
  create_time: {
    type: DataTypes.DATE,
    allowNull: false,
    defaultValue: DataTypes.NOW
  }
}, {
  tableName: 'tag',
  timestamps: false
});

module.exports = Tag;
```

```javascript
// server/models/RestaurantTag.js
const { DataTypes } = require('sequelize');
const { sequelize } = require('../config/database');

const RestaurantTag = sequelize.define('RestaurantTag', {
  id: {
    type: DataTypes.BIGINT,
    primaryKey: true,
    autoIncrement: true
  },
  restaurant_id: {
    type: DataTypes.BIGINT,
    allowNull: false
  },
  tag_id: {
    type: DataTypes.BIGINT,
    allowNull: false
  },
  create_time: {
    type: DataTypes.DATE,
    allowNull: false,
    defaultValue: DataTypes.NOW
  }
}, {
  tableName: 'restaurant_tag',
  timestamps: false,
  indexes: [
    {
      unique: true,
      fields: ['restaurant_id', 'tag_id']
    }
  ]
});

module.exports = RestaurantTag;
```

### 4.5 前端API请求示例

```javascript
// client/src/api/tag.api.js
import request from './request'

// 公开
export const getAllTags = (params) => {
  return request.get('/tags', { params })
}

export const getRestaurantTags = (restaurantId) => {
  return request.get(`/restaurants/${restaurantId}/tags`)
}

// 商家
export const addRestaurantTag = (restaurantId, tagId) => {
  return request.post(`/merchant/restaurants/${restaurantId}/tags`, { tagId })
}

export const removeRestaurantTag = (restaurantId, tagId) => {
  return request.delete(`/merchant/restaurants/${restaurantId}/tags/${tagId}`)
}

export const getMerchantRestaurantTags = (restaurantId) => {
  return request.get(`/merchant/restaurants/${restaurantId}/tags`)
}

// 管理员
export const createTag = (data) => {
  return request.post('/admin/tags', data)
}

export const updateTag = (tagId, data) => {
  return request.put(`/admin/tags/${tagId}`, data)
}

export const deleteTag = (tagId) => {
  return request.delete(`/admin/tags/${tagId}`)
}

export const getAdminTags = (params) => {
  return request.get('/admin/tags', { params })
}
```

---

## 五、models/index.js 关联更新说明

在现有 `server/models/index.js` 中新增以下关联关系：

```javascript
// 新增导入
const Dish = require('./Dish');
const Favorite = require('./Favorite');
const Like = require('./Like');
const Tag = require('./Tag');
const RestaurantTag = require('./RestaurantTag');

// 菜品关联
Restaurant.hasMany(Dish, { foreignKey: 'restaurant_id', as: 'dishes' });
Dish.belongsTo(Restaurant, { foreignKey: 'restaurant_id', as: 'restaurant' });

// 收藏关联
User.hasMany(Favorite, { foreignKey: 'user_id', as: 'favorites' });
Favorite.belongsTo(User, { foreignKey: 'user_id', as: 'user' });
Restaurant.hasMany(Favorite, { foreignKey: 'restaurant_id', as: 'favorites' });
Favorite.belongsTo(Restaurant, { foreignKey: 'restaurant_id', as: 'restaurant' });

// 点赞关联
User.hasMany(Like, { foreignKey: 'user_id', as: 'likes' });
Like.belongsTo(User, { foreignKey: 'user_id', as: 'user' });
Review.hasMany(Like, { foreignKey: 'review_id', as: 'likes' });
Like.belongsTo(Review, { foreignKey: 'review_id', as: 'review' });

// 标签关联（多对多）
Restaurant.belongsToMany(Tag, {
  through: RestaurantTag,
  foreignKey: 'restaurant_id',
  otherKey: 'tag_id',
  as: 'tags'
});
Tag.belongsToMany(Restaurant, {
  through: RestaurantTag,
  foreignKey: 'tag_id',
  otherKey: 'restaurant_id',
  as: 'restaurants'
});

// 新增导出
module.exports = {
  User,
  Restaurant,
  Review,
  Dish,
  Favorite,
  Like,
  Tag,
  RestaurantTag,
  sequelize: require('../config/database').sequelize
};
```

---

## 附录

### A. 新增文件清单

| 层级 | 文件 | 说明 |
| --- | --- | --- |
| Model | server/models/Dish.js | 菜品模型 |
| Model | server/models/Favorite.js | 收藏模型 |
| Model | server/models/Like.js | 点赞模型 |
| Model | server/models/Tag.js | 标签模型 |
| Model | server/models/RestaurantTag.js | 餐厅-标签关联模型 |
| Controller | server/controllers/dish.controller.js | 菜品控制器 |
| Controller | server/controllers/favorite.controller.js | 收藏控制器 |
| Controller | server/controllers/like.controller.js | 点赞控制器 |
| Controller | server/controllers/tag.controller.js | 标签控制器 |
| Route | server/routes/dish.routes.js | 菜品路由 |
| Route | server/routes/favorite.routes.js | 收藏路由 |
| Route | server/routes/like.routes.js | 点赞路由 |
| Route | server/routes/tag.routes.js | 标签路由 |
| Frontend API | client/src/api/dish.api.js | 菜品前端API |
| Frontend API | client/src/api/favorite.api.js | 收藏前端API |
| Frontend API | client/src/api/like.api.js | 点赞前端API |
| Frontend API | client/src/api/tag.api.js | 标签前端API |

### B. 路由注册说明

在 `server/routes/index.js` 中新增：

```javascript
const dishRoutes = require('./dish.routes');
const favoriteRoutes = require('./favorite.routes');
const likeRoutes = require('./like.routes');
const tagRoutes = require('./tag.routes');

router.use('/merchant/dishes', dishRoutes);       // 商家菜品管理
router.use('/dishes', dishRoutes);                 // 公开菜品查询
router.use('/favorites', favoriteRoutes);           // 收藏
router.use('/likes', likeRoutes);                   // 点赞
router.use('/admin/tags', tagRoutes);               // 管理员标签管理
router.use('/tags', tagRoutes);                     // 公开标签查询
router.use('/merchant/restaurants', tagRoutes);     // 商家餐厅标签管理
```

### C. 新增路由文件

#### server/routes/dish.routes.js
```javascript
const express = require('express');
const router = express.Router();
const dishController = require('../controllers/dish.controller');
const { authenticate, authorize } = require('../middleware/auth');

// 公开接口
router.get('/:restaurantId/dishes', dishController.getPublicList);
router.get('/:id', dishController.getDetail);

// 商家接口
router.post('/', authenticate, authorize('merchant'), dishController.create);
router.put('/:id', authenticate, authorize('merchant'), dishController.update);
router.delete('/:id', authenticate, authorize('merchant'), dishController.remove);
router.get('/', authenticate, authorize('merchant'), dishController.getMerchantList);

module.exports = router;
```

#### server/routes/favorite.routes.js
```javascript
const express = require('express');
const router = express.Router();
const favoriteController = require('../controllers/favorite.controller');
const { authenticate, authorize } = require('../middleware/auth');

router.use(authenticate, authorize('student'));

router.post('/', favoriteController.add);
router.delete('/:id', favoriteController.remove);
router.get('/', favoriteController.getList);
router.get('/check', favoriteController.check);

module.exports = router;
```

#### server/routes/like.routes.js
```javascript
const express = require('express');
const router = express.Router();
const likeController = require('../controllers/like.controller');
const { authenticate, authorize } = require('../middleware/auth');

router.post('/', authenticate, authorize('student'), likeController.add);
router.delete('/:id', authenticate, authorize('student'), likeController.remove);
router.get('/check', authenticate, authorize('student'), likeController.check);

module.exports = router;
```

#### server/routes/tag.routes.js
```javascript
const express = require('express');
const router = express.Router();
const tagController = require('../controllers/tag.controller');
const { authenticate, authorize } = require('../middleware/auth');

// 公开接口
router.get('/', tagController.getAllTags);
router.get('/restaurants/:restaurantId/tags', tagController.getRestaurantTags);

// 商家接口（餐厅标签管理）
router.post('/restaurants/:restaurantId/tags', authenticate, authorize('merchant'), tagController.addRestaurantTag);
router.delete('/restaurants/:restaurantId/tags/:tagId', authenticate, authorize('merchant'), tagController.removeRestaurantTag);
router.get('/restaurants/:restaurantId/tags', authenticate, authorize('merchant'), tagController.getMerchantRestaurantTags);

// 管理员接口（标签CRUD）
router.post('/', authenticate, authorize('admin'), tagController.create);
router.put('/:id', authenticate, authorize('admin'), tagController.update);
router.delete('/:id', authenticate, authorize('admin'), tagController.remove);
router.get('/admin/all', authenticate, authorize('admin'), tagController.getAdminList);

module.exports = router;
```

### D. 开发顺序建议

| 阶段 | 内容 | 预估工时 | 并行可能性 |
| --- | --- | --- | --- |
| 第一阶段 | 数据库建表（dish、favorite、likes、tag、restaurant_tag） | 0.5天 | 可提前 |
| 第二阶段 | 后端Models（Dish.js、Favorite.js、Like.js、Tag.js、RestaurantTag.js）+ models/index.js关联 | 0.5天 | 与第一阶段并行 |
| 第三阶段 | 后端Dish控制器、路由、前端API | 1天 | 与第四/五/六阶段并行 |
| 第四阶段 | 后端Favorite控制器、路由、前端API | 0.5天 | 与第三/五/六阶段并行 |
| 第五阶段 | 后端Like控制器、路由、前端API | 0.5天 | 与第三/四/六阶段并行 |
| 第六阶段 | 后端Tag控制器、路由、前端API | 1天 | 与第三/四/五阶段并行 |
| 第七阶段 | 前端UI组件开发（菜品管理、收藏按钮、点赞按钮、标签选择器） | 2天 | 与第三~六阶段并行（Mock数据） |
| 第八阶段 | 集成测试与联调 | 1天 | 串行 |

**总计预估：** 约3-4人天（含前后端并行开发）

### E. 图片上传说明

本文档中菜品图片字段（dish.image）和餐厅图片字段（restaurant.image）的图片上传复用现有 `/api/v1/upload` 接口，前端上传后取返回的URL传入对应字段。

```
POST /api/v1/upload
请求：multipart/form-data 格式，文件字段名 file
返回：{"code":200,"message":"上传成功","data":{"url":"http://..."}}
```

---

—— 文档结束 ——
